[project]
name = "daon-user-workspace"
version = "0.0.0"
requires-python = "==3.14.3"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["services/api", "services/local-service"]

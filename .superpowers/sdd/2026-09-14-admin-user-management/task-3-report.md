# Task 3 Web UI 구현 보고

COMPLETED | issue_id=R1-ADMIN-USER-MANAGEMENT-01 | 승인된 Task 3 범위에서 system-admin 전용 사용자 관리 진입·독립 `/admin` 콘솔·강제 `/password-change` 흐름·조직 탐색 제거를 TDD로 구현했다. Notebook Home과 선택 Notebook은 세션의 `password_change_required`를 Notebook API 호출 전에 검사하며, 설정의 `사용자 관리`는 `is_system_admin=true`일 때만 표시한다. | `apps/web/components/admin-user-console.jsx`, `apps/web/lib/admin-users-api.js`, `apps/web/components/password-change-workspace.jsx`, `apps/web/app/password-change/page.jsx`를 생성하고 `/admin`, session projection, auth client, BFF allowlist, Notebook 설정 메뉴·routing, CSS, test harness와 Node/component tests를 변경했다. 조직 UI 파일은 삭제하지 않았고 현재 Web 탐색의 `/organization-admin`·`/organization/join` 링크만 제거했다. 테스트가 만든 `.admin-console-JRigQd` 등 임시 bundle 디렉터리 4개를 정확한 경로 확인 후 삭제했으며 최종 잔류는 0개다. | `node --test scripts/tests/notebook-api.test.mjs scripts/tests/admin-users-bff.test.mjs scripts/tests/admin-user-management-ui.test.mjs scripts/tests/admin-user-management-react.test.mjs scripts/tests/phase-e-product-assembly.test.mjs scripts/tests/notebook-home-ui.test.mjs scripts/tests/notebook-home-react.test.mjs scripts/tests/api-bff-runtime.test.mjs scripts/tests/auth-pane.test.mjs` → 70/70 PASS; `npm run lint:workspace` → PASS(16 files); `npm run build --workspace @daon-user/web` → PASS, Next 17 routes 생성 및 Web product boundary 467 files/위반 0/경계 오류 0; `git diff --check` → PASS. 추가 탐색용 Web/UI/BFF 파일명 기반 묶음은 build 전에 146개 중 144개 PASS였고, 하나는 기존 `product-ui-boundary`의 Vite/Rolldown external React `require` test-harness 오류, 하나는 build 전 `.next`/desktop build 산출물 부재에 따른 fail-closed였다. | 실제 브라우저·Network·API 403 통합 검증은 승인 계획 Task 5 범위로 이번 Task 3에서는 수행하지 않았다. 범위 밖 기존 `product-ui-boundary` React route test-harness 오류는 수정하지 않았다. | main agent가 Task 3 commit을 검토하고 승인 계획의 다음 Task로 진행한다. merge·push·PR·배포 판단은 이번 작업 범위가 아니다.

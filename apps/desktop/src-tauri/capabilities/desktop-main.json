{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-main",
  "description": "Bundled UI only; no command, network, filesystem, process, shell, or plugin access.",
  "windows": ["main"],
  "permissions": []
}

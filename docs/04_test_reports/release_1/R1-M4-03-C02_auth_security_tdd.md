# R1-M4-03-C02 인증 보안 TDD 증거

- 기록일: 2026-08-05
- 범위: 로컬 가입 계정 존재 노출 방지, 이메일 인증 재전송·비밀번호 재설정 메일 요청 제한
- 기준 작업: R1-M4-03-C02

## RED

`tests/test_identity_local_auth.py`에 다음 재현 테스트를 먼저 추가했다.

- 같은 ID 또는 이메일로 재가입해도 계정 존재 여부를 응답으로 노출하지 않는다.
- 가입 인증 메일 재전송과 비밀번호 재설정 메일의 즉시 반복 요청은 `RATE_LIMITED` 429로 거부된다.

실행 결과: `2 passed, 2 failed`. 실패 원인은 기존 `SIGNUP_ALREADY_EXISTS` 409 응답과 메일 재요청 제한 부재였다. RED 기준선은 커밋 `2f542b0`으로 보존했다.

## GREEN

- 중복 가입은 최초 가입과 같은 accepted 응답을 반환하되, 계정이나 메일을 추가로 생성하지 않도록 했다.
- 비밀번호 해싱을 존재 여부 조회 전에 수행해 응답 시간 차이를 줄였다.
- 계정별 메일 요청은 60초 재요청 대기와 1시간당 최대 3회를 적용했다.

표적 테스: `4 passed in 0.67s`.

인증 회귀: `11 passed, 6 subtests passed in 1.46s`.

정적 검증: `python -m compileall -q src` 통과.

## 보장과 남은 검증

- 외부 요청자는 기존 계정 존재 여부를 가입·인증 재전송·비밀번호 재설정 응답으로 확정할 수 없다.
- 토큰은 기존처럼 digest만 DB에 보존한다.
- SMTP 실제 값이 아직 등록되지 않아 실제 메일 수신·링크 완료 검증은 대기 상태다.

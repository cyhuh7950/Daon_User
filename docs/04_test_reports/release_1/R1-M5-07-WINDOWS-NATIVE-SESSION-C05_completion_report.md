# R1-M5-07 Windows Native Session 보안 보정 완료 보고

status | issue_id | 수행한 작업 | 생성·변경한 결과 | 테스트 결과 | 미해결 사항 | 다음으로 필요한 판단
COMPLETED | R1-M5-07-WINDOWS-NATIVE-SESSION-C04-I001 | C04 내부 보안 검토의 Critical Redirect와 Important 응답 제한·Secret 수명주기·Vault 삭제·Refresh 동시성·결정론적 test 경계를 C05 승인 계약 안에서 보정했다. | reqwest는 Redirect를 따르지 않고 3xx를 fail-close하며 5초 connect·20초 request timeout, 128KiB Content-Length·chunk 제한, JSON Content-Type·unknown field·RFC3339 UTC 형식·Cookie 거부를 적용한다. `zeroize 1.9.0` direct pin과 Drop/Wipe를 적용하고 CredReadW Blob은 복사 뒤 CredFree 전 zeroize한다. Vault 삭제 실패는 pending fail-close 상태가 되어 인증 status를 반환하지 않으며, Runtime은 blocking Win32 호출을 `spawn_blocking`으로 격리하고 Refresh read→HTTP→write를 async Mutex로 직렬화한다. Wrapper는 Native 계약 test도 포함한다. | RED: 신규 `NativeHttpResponse`·`NativeRefreshFlow`·Transport/Vault Port 부재 `E0432` 확인. GREEN: `node scripts/run-isolated-desktop-cargo.mjs test`에서 lib 16/16, Local Service 4/4, Native Session 12/12, 총 32/32 PASS. Windows Generic Credential의 실제 corrupt Blob 기록→read fail-close→Target 삭제도 PASS. `npm run verify:desktop-lint`, `git diff --check`, `GEN_EXISTS=False`, local_service diff 0건 PASS. Node 정적 test는 Native 보안 항목 포함 12 PASS. | `desktop-tauri-shell.test.mjs`의 기존 PostCSS 환경 test 1건은 현재 node_modules가 Next/Vite/PostCSS를 extraneous으로 보고해 실패한다. package lock·node_modules는 C05 금지 범위여서 변경하지 않았다. 실제 Password·운영 Credential·Browser·설치 실행·배포는 수행하지 않았다. reqwest 내부 request buffer처럼 애플리케이션이 소유하지 않는 라이브러리 복사본은 zeroize를 보장할 수 없다. | 어울1의 보안 diff 검토 후 Commit/Push 여부를 판단한다. 이 C05 결과는 자동 테스트 근거이며 실제 설치형 로그인 PASS는 아니다.
